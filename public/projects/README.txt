Survesy — project screenshot
============================

The Projects section references one local screenshot for Survesy:

  survesy-dashboard.png   -> the Dashboard (KPIs + Response Trend + Survey Status)

Tips:
- PNG or JPG both work (keep the .png filename or update the path in code).
- Recommended width ~1200-1600px; compress to keep it under ~400 KB.
- Until this file exists, the card shows a placeholder image automatically,
  so nothing looks broken.
